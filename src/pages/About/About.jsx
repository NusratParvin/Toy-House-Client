import React from 'react';
import useTitle from '../../hook/useTitle';

const About = () => {
    useTitle('About Us')
    return (
        <div>
           about 
        </div>
    );
};

export default About;